# LDAP 登录（企业目录认证）产品需求文档 (PRD)

**创建时间**: 2026-08-27
**优先级**: P1
**所属域**: auth

---

## 1. 相关用户故事

> 详细故事与验收标准请查看 `docs/user-stories/auth/support-ldap.md`。

### 1.1 相关故事

| US-ID | 标题 | 角色 | 优先级 | 来源 |
|-------|------|------|--------|------|
| US-LD-001 | 用企业账号（LDAP）登录 | Regular User | P0 | `docs/user-stories/auth/support-ldap.md` |
| US-LD-002 | 首次企业账号登录自动创建账号 | Regular User | P0 | `docs/user-stories/auth/support-ldap.md` |
| US-LD-003 | Realm 管理员配置和管理本 Realm 的 LDAP 目录 | Realm Admin | P0 | `docs/user-stories/auth/support-ldap.md` |
| US-LD-004 | 企业账号登录与其他登录及安全能力共存 | Regular User | P1 | `docs/user-stories/auth/support-ldap.md` |

表达既有登录、注册、同意与下游授权语义，不复制验收标准的既有故事：

- `docs/user-stories/core/regular-user.md`（US-RU-002 登录、US-RU-003 OAuth 第三方登录）
- `docs/user-stories/auth/third-party-app.md`（US-TP-001 OAuth 授权码登录）
- `docs/user-stories/core/legal-consent-account-deletion.md`（登录即同意模型）
- `docs/user-stories/auth/totp.md`（二因素验证共存）
- `docs/user-stories/auth/email-otp-login.md`（US-EO-003 管理员配置替代登录方式的管理形态参照）

### 1.2 优先级汇总

| 优先级 | 数量 | 关键故事 |
|--------|------|----------|
| P0 | 3 | US-LD-001 企业账号登录、US-LD-002 首登自动建号、US-LD-003 管理员目录配置 |
| P1 | 1 | US-LD-004 与既有登录/安全能力共存 |

---

## 2. 范围界定

### 2.1 包含功能

- **租户级 LDAP 目录配置**：Realm Admin 为本 Realm 配置企业目录（服务器地址与加密方式、可选私有 CA 证书 PEM（`caCertPem`，与系统信任库叠加用于企业内部 CA 签发的目录证书）、Base DN、服务账号或匿名搜索、用户搜索过滤模板、登录标识/邮箱/显示名的属性映射）并启用/停用。
- **登录页企业账号入口**：按 Realm 启用状态显隐的"企业账号登录"表单（企业用户名 + 密码）。
- **目录认证**：后端以标准 search-then-bind 模式完成验证（服务账号检索用户条目 → 用用户凭据向目录验证），兼容 Active Directory 与 OpenLDAP 等标准 LDAPv3 目录。
- **首次登录自动建号与身份链接**：首次认证成功自动创建无本地密码的账号并链接目录身份；用户匹配按 目录身份 → 邮箱 → 创建 逐级进行。
- **完整继承现有登录管线**：人机验证、IP+标识符限流、二因素验证、协议同意、下游授权码分支、会话签发、审计（认证方式标记为企业目录）。
- **公共配置暴露启用状态**：登录页可按租户感知企业账号登录是否启用。

### 2.2 不包含功能 (Out of Scope)

- **LDAP 组 → Herald 角色映射**：目录用户组不自动映射为 Herald 角色/权限（如有需要独立立项）。
- **后台目录同步**：不做账号/属性的批量或增量同步（如有需要独立立项）。
- **Windows 桌面单点登录**：不做 SPNEGO/Kerberos 内网免密自动登录，仅登录页表单认证。
- **SASL 专用机制**：不做 GSSAPI/NTLM 等机制，仅标准用户名密码验证。
- **多目录/故障转移**：v1 每 Realm 单一目录配置。
- **本地凭据缓存或目录离线回退**：目录不可达时企业账号登录即不可用（用户可走其他登录方式）。
- **代理修改目录内密码**：企业密码的修改/重置走企业目录自身渠道，Herald 不写目录。
- **连接池化等性能优化**：如成为瓶颈另行立项评估。
- **目录连接测试端点与主动健康检查**：当前保存配置只做结构与加密信道校验，真实连通性、凭据和 Base DN 在登录使用时验证；管理端没有专用 test-connection 反馈通道。

### 2.3 依赖项

- **本 Realm 既有配置管理能力**：目录配置复用既有租户配置管理（存储、凭据敏感标记与 secrets 读取路径），不新建独立配置体系。
- **现有登录管线**：Client App 上下文解析、人机验证（按 Client App 配置）、IP+标识符限流、TOTP/Passkey 二因素探测、协议同意闸门、下游授权码分支、token 签发（详见 [docs/prd/auth/oauth.md](oauth.md)、[docs/prd/auth/email-otp-login.md](email-otp-login.md) 的既有语义）。
- **既有身份链接与自动建号能力**：第三方登录 JIT 建号范式（无本地密码建号 + 身份链接 + 占位邮箱策略）。
- **协议同意模型**：复用"登录即同意"语义（[docs/prd/core/legal-consent-account-deletion.md](../core/legal-consent-account-deletion.md)）。
- **审计日志能力**：登录事件按认证方式记录（[docs/prd/core/audit.md](../core/audit.md)）。
- **前端登录页状态开关机制**：按 Realm 公共配置显隐登录方式（邮箱验证码/Passkey 入口同型）。

---

## 3. 需求概述

### 3.1 功能描述

企业客户的员工账号密码统一保管在企业目录（LDAP / Active Directory）中。当前这些租户的最终用户只能在 Herald 另建一套账号密码：注册有摩擦、两套密码并存带来管理负担，也无法体现"目录才是企业身份权威"的现实。

本功能让 Realm Admin 配置本企业目录后，员工在登录页用既有企业账号密码完成认证：首次登录自动创建 Herald 账号并关联目录身份，后续登录识别为同一账号。LDAP 在本能力中定位为**凭据权威而非账号唯一来源**——Herald 仍是账号与授权的记录源，不回写、不同步目录；这与已排除的目录同步（见 §2.2）形成清晰边界。

### 3.2 关键特性

- **企业账号一次登录**：首次登录即完成开户与登录，无需管理员逐个预建账号。
- **受控供给**：管理员启用目录即表达对该目录供给的授权，不受 Realm 公开注册开关限制——"关闭公开自注册、员工走目录登录"正是企业主场景。
- **完整安全管线继承**：企业账号登录复用既有登录的全部安全能力，不是绕过二因素/限流/审计的简化后门。
- **平滑降级**：停用目录配置不影响已创建账号，员工可继续使用已绑定的其他登录方式。
- **兼容主流目录**：Active Directory 与 OpenLDAP 等标准 LDAPv3 目录通过可配置属性映射通用支持。

---

## 4. 业务规则与状态

### 4.1 业务规则

- **租户隔离**：目录配置、用户匹配与账号创建均限定在本 Realm 内；跨 Realm 的配置访问或数据访问被拒绝。
- **每 Realm 单一目录配置（v1）**：配置含启用开关；开关状态作为公共信号供登录页显隐入口。
- **凭据信道加密（硬规则）**：目录连接必须走加密信道（LDAPS 或 StartTLS）；不加密的明文连接配置一律拒绝保存。企业账号密码只允许在加密信道中传输。
- **认证模式**：search-then-bind——以服务账号（或匿名搜索，若目录允许）在 Base DN 下检索唯一用户条目，再用用户提交的凭据完成验证；搜索命中 0 个或多于 1 个条目时认证失败，不做猜测式绑定。
- **注入防护**：用户输入不得直接拼入目录搜索条件；搜索过滤模板仅来自管理员配置并经格式校验。
- **用户匹配策略**：目录身份（用户条目 DN）→ 邮箱 → 创建。目录邮箱由企业管理员在目录中维护，视为可信来源，允许据此登入既有账号；保证同一员工先有本地账号、后接目录时不产生重复账号。
- **JIT 建号授权**：首次目录认证成功即自动建号，**不受 Realm 公开注册开关限制**——管理员启用目录已构成对该目录供给的授权（与既有"自动注册不得绕过注册政策"规则的有意差异，理由见 DEC-support-ldap-007）。
- **无本地密码**：目录建号账号没有本地密码，密码登录表单对其不适用；用户后续自行设置本地密码后可恢复密码登录。
- **邮箱缺失容忍**：目录未返回邮箱时以占位邮箱建号且不标记已验证；后续登录按目录身份命中，不依赖邮箱。
- **账号状态优先**：目录认证成功但 Herald 账号被禁用时拒绝登录。
- **防枚举**：登录失败返回与既有密码登录一致的泛化错误，不区分"目录里没有这个人"与"密码错误"。
- **防滥用**：继承人机验证（按当前 Client App 的 Turnstile 配置执行）与 IP+标识符限流，不降低既有阈值。
- **管线继承**：二因素验证（TOTP/Passkey）、协议同意（登录即同意）、下游授权码分支、会话签发与审计（认证方式标记为企业目录）全部继承既有登录管线语义。
- **共存原则**：企业账号登录与其他登录方式并列共存、互不影响；停用后平滑降级。

### 4.2 关键状态与异常

- **密码错误 / 目录无此用户** → 泛化"账号或密码错误"，允许重试并受限流约束。
- **目录不可达 / 响应超时** → "登录暂时不可用，请稍后再试"类提示，不暴露目录地址等内部细节；其他登录方式不受影响；故障计入审计与日志供管理员排查。
- **搜索无唯一命中** → 按泛化认证失败处理（含搜索结果多于一条的情况）。
- **目录配置错误**（服务账号凭据失效、Base DN 错误等）→ 企业账号登录不可用，错误通过登录失败和服务端日志暴露；保存时不主动连接目录，当前无管理端连接测试反馈。
- **账号被禁用** → 目录认证成功仍拒绝登录，明确提示账号已被禁用。
- **Realm 未启用 LDAP** → 登录页不展示入口；直接提交企业账号登录请求被拒绝，不创建任何账号或会话。
- **目录邮箱与既有账号一致** → 关联既有账号登录，不重复建号（§4.1 匹配策略）。
- **目录未返回邮箱** → 占位邮箱建号（未验证），登录成功。

---

## 5. 功能需求

### 5.1 核心需求

- **FR-1（租户级目录配置管理）**：Realm Admin 可为本 Realm 配置、修改、停用 LDAP 目录（服务器地址与加密方式、私有 CA 证书 PEM（`caCertPem`，可选）、Base DN、服务账号或匿名搜索、用户搜索过滤模板、属性映射）；服务账号凭据按敏感信息保护，仅限本 Realm 管理员访问。
- **FR-2（登录页企业账号入口）**：登录页按 Realm 启用状态展示"企业账号登录"表单（企业用户名 + 密码）；未启用的 Realm 不展示入口。
- **FR-3（目录认证）**：后端以 search-then-bind 完成企业账号验证；用户密码只在加密信道传输、不落任何存储、验证即弃。
- **FR-4（身份匹配与 JIT 建号）**：按 目录身份 → 邮箱 → 创建 匹配账号；首次目录认证成功自动建号（无本地密码），不受 Realm 公开注册开关限制；建号前须满足当前 Realm 生效协议的同意表达（如有）。
- **FR-5（安全管线继承）**：企业账号登录完整继承人机验证、IP+标识符限流、二因素验证、协议同意、下游授权码分支、会话签发与审计。
- **FR-6（公共配置暴露启用状态）**：向登录页暴露该 Realm 是否启用企业账号登录的状态信号，不包含任何目录配置细节或凭据。

### 5.2 验收目标

- Realm 配置并启用目录后，员工用企业账号一次操作完成登录；首次自动建号，再次登录命中同一账号，不重复建号。
- 先有本地账号（邮箱一致）的员工首次企业账号登录关联到既有账号登录成功，不产生重复账号。
- 公开注册关闭的 Realm，员工首次企业账号登录仍自动建号并可完成登录。
- 目录不可达时登录页给出可理解提示且其他登录方式可用；管理员停用后入口消失，已建账号与其已绑定的其他登录方式不受损。
- 不加密的明文目录地址配置被拒绝保存；服务账号密码在任何配置查看、公共配置与日志中不可见。
- 登录失败不泄露"目录里没有这个人"与"密码错误"的区别。
- 已绑定 TOTP 的用户企业账号登录被要求二因素验证；从第三方应用发起登录时下游授权链路正常完成。
- 审计事件在 `details.method=ldap` 标记认证方式，可在详情中识别；当前查询 API 不提供按认证方式的独立筛选参数。
- 未启用 LDAP 的 Realm 一切企业账号登录尝试被拒绝且不创建账号。

---

## 6. API 相关约束

**适用性**: 适用

- **能力边界**：新增一个未认证身份的企业账号认证能力；管理端复用本 Realm 既有配置管理能力（不新增专用管理接口面）；公共配置新增企业账号登录启用状态信号。不新增其他高权限接口。
- **访问控制原则**：企业账号认证为公开端点（无需已认证身份），但必须完成人机验证与限流；目录配置的读写仅限拥有本 Realm 设置管理权限（`settings.view`/`settings.manage`）的管理员。
- **租户/realm 数据边界**：认证请求绑定 Realm（与 Client App 上下文一致）；用户匹配与账号创建限定在当前 Realm 内；跨 Realm 配置访问被拒绝。
- **兼容性**：与现有密码登录、邮箱验证码登录、第三方 OAuth 登录、TOTP/Passkey、协议同意模型共存；兼容下游 Authorization Code 流程；公共配置新增信号为向后兼容变更。
- **安全约束**：服务账号凭据与用户密码不出现在日志、tracing 与公共配置中；用户密码不落存储、不缓存；泛化错误响应防枚举。

> 端点清单、请求/响应参数、状态码矩阵不在 PRD 承载范围，下沉到技术设计。

---

## 7. 前端/交互约束

**适用性**: 适用

- **页面入口**：登录页新增"企业账号登录"入口，与密码登录、邮箱验证码等并列；入口可见性由 Realm 启用状态决定；非 Realm 镜像登录页同步支持。
- **关键交互**：用户选择"企业账号登录" → 输入企业用户名与密码（完成人机验证，若启用）→ 认证成功后进入既有后续流程（二因素验证/协议同意/完成登录）；首次登录自动建号，无额外注册步骤（Realm 要求协议同意时除外）。
- **状态反馈**：
  - 认证失败（用户不存在/密码错误）→ 与密码登录一致的泛化错误提示。
  - 目录不可用 → "登录暂时不可用，请稍后再试"，并提供其他登录方式回退。
  - 账号被禁用 → 明确禁用提示。
  - 管理端保存不加密明文目录地址 → 拒绝并提示必须使用加密连接。
- **降级**：Realm 未启用 → 入口不展示；管理员停用 → 入口消失，已建账号用户经其他登录方式登录。
- **权限可见性**：目录配置入口仅对本 Realm 拥有设置管理权限的管理员可见；服务账号密码回显为掩码或不回显。

> 组件实现形态（与邮箱验证码登录入口同型）由前端设计承接。

---

## 8. 已确认决策

| Decision ID | 状态 | 决策项 | 结论 | PRD 落点 | 来源 |
|---|---|---|---|---|---|
| `DEC-support-ldap-001` | Applied | 功能范围 | 仅 LDAP 登录认证；不做组→角色映射、不做后台目录同步（各自独立立项） | §2.1/§2.2 | `.ai/decision-log/support-ldap.md` |
| `DEC-support-ldap-002` | Applied | JIT 建号 | 首次目录认证成功自动建号：无本地密码 + 目录身份链接；邮箱缺失用占位且不标记已验证 | §4.1/FR-4 | `.ai/decision-log/support-ldap.md` |
| `DEC-support-ldap-003` | Applied | 桌面 SSO | 不做 SPNEGO/Kerberos 桌面单点登录，仅登录页表单认证 | §2.2 | `.ai/decision-log/support-ldap.md` |
| `DEC-support-ldap-004` | Applied | LDAP 客户端技术选型 | ldap3 + rustls 后端，不引入 openssl/native-tls 系依赖；技术细节不由 PRD 承载 | 设计层约束 | `.ai/decision-log/support-ldap.md` |
| `DEC-support-ldap-005` | Applied | 配置存储 | 复用本 Realm 既有配置管理与凭据保护路径（服务账号密码按敏感信息标记），不新建独立配置体系 | §2.3/FR-1 | `.ai/decision-log/support-ldap.md` |
| `DEC-support-ldap-006` | Applied | 认证接入形态 | 专用认证路径完整镜像既有登录管线（不在现有密码登录路径内分支） | FR-3/FR-5 | `.ai/decision-log/support-ldap.md` |
| `DEC-support-ldap-007` | Applied | JIT 与注册政策 | 自动建号不受 Realm 公开注册开关门控；管理员启用目录即供给授权（与既有"自动注册不得绕过注册政策"规则的有意差异，依据同类产品通行做法） | §4.1/FR-4 | `.ai/decision-log/support-ldap.md` |
| `DEC-support-ldap-008` | Applied | 用户匹配策略 | 目录身份（DN）→ 邮箱 → 创建；目录邮箱视为可信（等价已验证）来源，允许据此登入既有账号 | §4.1/FR-4 | `.ai/decision-log/support-ldap.md` |
| `DEC-support-ldap-009` | Applied | 唯一命中规则 | 用户条目搜索必须唯一命中（0 条或多条均认证失败），不做猜测式绑定 | §4.1/§4.2 | `.ai/decision-log/support-ldap.md` |

> 以上均为已确认并应用的 Active Decision，无 Deferred Questions；决策依据与重开条件见决策账本。

---

## 9. 参考资料

- 用户故事：`docs/user-stories/auth/support-ldap.md`（US-LD-001/002/003/004）
- 依赖的基线用户故事：`docs/user-stories/core/regular-user.md`（US-RU-002/003）、`docs/user-stories/auth/third-party-app.md`（US-TP-001）、`docs/user-stories/auth/totp.md`、`docs/user-stories/auth/email-otp-login.md`（US-EO-003 管理形态参照）、`docs/user-stories/core/legal-consent-account-deletion.md`（登录即同意）
- 既有登录管线与替代第一因子基线：[邮箱验证码登录](email-otp-login.md)、[苹果手机 App native 登录](support-mobile-apple-login.md)
- 外部身份 JIT 与占位邮箱范式：[微信 OAuth](wechat-oauth.md)
- Client App 级人机验证：[Client App 管理](../integration/client-app.md)
- 协议同意模型：[合规适配](../core/legal-consent-account-deletion.md)
- 审计：[Audit 审计日志](../core/audit.md)
- 决策账本：`.ai/decision-log/support-ldap.md`
- 技术预研：`.ai/tech-research/support-ldap.md`
- 角色定义：`docs/user-stories/_roles.md`
